
properties([disableConcurrentBuilds()])

pipeline {
    agent any

    triggers {
        pollSCM('H * * * *')
    }

    stage("Build & Tests") {
            steps {
                script {
                        sh './gradlew clean test --no-daemon' //run a gradle task
                }
            }
    }


}